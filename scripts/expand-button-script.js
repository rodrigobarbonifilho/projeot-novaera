let buttons = document.querySelectorAll("div.buttons>button");
let yesButtons = [buttons[0], buttons[2]]
let noButtons = [buttons[1], buttons[4]]
let phoneContainer = document.getElementById("phone-contact-container-js");
let emailContainer = document.getElementById("email-contact-container-js");

yesButtons.forEach((btn) => {
    btn.addEventListener("click", () => {
        if (btn.classList)
    })
})